cp is used to copy a file and make a duplicate.

cp takes two parameters, firstly the name of the file to be copied and second the location to copy the file to.

If location is unchanged, second parameter is used for new filename.

e.g. cp myFile ./newDir

e.g. cp newFile newerFile
